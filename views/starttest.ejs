<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Start Test</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anta&display=swap" rel="stylesheet">
</head>
<style>
    .anta-regular {
        font-family: "Anta", sans-serif;
        font-weight: 400;
        font-style: normal;
    }

    body {
        font-family: "Anta", sans-serif;
        background-color: #122028;
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .container {
        text-align: center;
    }

    h1 {
        margin-bottom: 20px;
        color: #fff;
    }

    .exam-info {
        margin-bottom: 20px;
        color: #fff;
    }

    .start-btn {
        padding: 15px 30px;
        font-size: 1.2em;
        background-color: #080683;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .start-btn:hover {
        background-color: #060460;
    }

    .cancel-btn {
        padding: 15px 30px;
        font-size: 1.2em;
        background-color: #e74c3c;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
        margin-right: 10px;
    }

    .cancel-btn:hover {
        background-color: #c0392b;
    }
</style>

<body>
    <div class="container">
        <h1>Start Test</h1>
        <div class="exam-info">
            <p><strong>Name:</strong> <%= exam.examName %></p>
            <p><strong>Start Time:</strong> <%= new Date(exam.startTime).toLocaleDateString() %> <%= new Date(exam.startTime).toLocaleTimeString() %></p>
            <p><strong>End Time:</strong> <%= new Date(exam.endTime).toLocaleTimeString() %></p>
        </div>
        <button class="start-btn" onclick="startTest('<%= test._id %>')">Start Test</button>
        <button class="cancel-btn" onclick="goBack()">Back</button>
    </div>

    <script>
        function startTest(testId) {
            window.location.href = `/testing/${testId}`;
        }

        function goBack() {
            window.history.back();
        }
    </script>
</body>

</html>