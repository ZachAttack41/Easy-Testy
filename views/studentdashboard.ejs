<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard - EasyTest</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Anta&display=swap" rel="stylesheet">
</head>
<style>
    .anta-regular {
        font-family: "Anta", sans-serif;
        font-weight: 400;
        font-style: normal;
    }

    body {
        font-family: "Anta", sans-serif;
        background-color: #122028;
        margin: 0;
        padding: 0;
    }

    header {
        background-color: #0a1116;
        padding: 20px;
        color: #fff;
        text-align: center;
        font-size: 2em;
    }

    section {
        margin: auto;
        padding: 20px;
        font-family: "Anta", sans-serif;
        background-color: #122028;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        text-align: center;
    }

    h2 {
        color: #ffffff;
        font-size: 1.5em;
        margin-right: 22px;
    }

    ul {
        list-style: none;
        padding: 0;
        font-size: 1.5em;
    }

    li {
        margin-bottom: 10px;
    }

    a {
        color: #ffffff;
        text-decoration: none;
        margin-right: 20px;
        text-align: center;
    }

    a:hover {
        text-decoration: underline;
    }

    .join-class-btn {
        display: inline-block;
        margin-top: 20px;
        padding: 15px 30px;
        font-size: 1.2em;
        text-align: center;
        text-decoration: none;
        color: #fff;
        background-color: #080683; /* Darker blue color */
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .join-class-btn:hover {
        background-color: #060460; /* Even darker blue color on hover */
    }

    .logout-btn {
        display: inline-block;
        margin-top: 20px;
        padding: 15px 30px;
        font-size: 1.2em;
        text-align: center;
        text-decoration: none;
        color: #fff;
        background-color: #e74c3c; /* Red color */
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .logout-btn:hover {
        background-color: #c0392b; /* Darker red color on hover */
    }
</style>

<body>
    <header>
        <h1>Welcome, <%= username %>!</h1>
    </header>

    <section>
        <h2>Your Classes:</h2>
        <% if (classes.length > 0) { %>
            <ul>
                <% classes.forEach(classItem => { %>
                    <li>
                        <a href="/studentclassview?classId=<%= classItem._id %>&userId=<%= userId %>">
                            <%= classItem.className %> - Period <%= classItem.period %>
                        </a>                        
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <p>No classes found.</p>
        <% } %>
        <a href="/joinclass?userId=<%= userId %>" class="join-class-btn">Join Class</a>
        <a href="/studentlogin" class="logout-btn">Logout</a>
        <div class="create-test"></div>
    </section>
</body>

</html>