<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam Details</title>
    <style>
        body {
            font-family: "Anta", sans-serif;
            background-color: #122028;
            margin: 0;
            padding: 0;
            color: #ffffff;
        }

        header {
            background-color: #0a1116;
            padding: 20px;
            text-align: center;
            font-size: 2em;
        }

        section {
            margin: 25px auto;
            padding: 25px;
            background-color: #0a1116;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.1);
            width: 80%;
            max-width: 800px;
            border-radius: 25px;
            position: relative;
            text-align: center;
        }

        h1 {
            color: #ffffff;
        }

        form {
            margin-top: 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            font-size: 2.5em;
            margin-bottom: 10px;
            color: #ffffff;
        }

        input[type="datetime-local"] {
            padding: 15px;
            font-size: 1.2em;
            border-radius: 5px;
            border: 1px solid #ccc;
            margin-bottom: 20px;
            width: 100%;
            max-width: 400px;
            box-sizing: border-box;
        }

        button[type="submit"] {
            padding: 15px 30px;
            font-size: 1.2em;
            text-align: center;
            text-decoration: none;
            color: #fff;
            background-color: #084321;
            border-radius: 5px;
            transition: background-color 0.3s;
            border: none;
            cursor: pointer;
            margin-top: 20px;
        }

        button[type="submit"]:hover {
            background-color: #063118;
        }
    </style>
</head>

<body>
    <header>
        <h1>Exam Details</h1>
    </header>

    <section>
        <form action="/confirmassign/<%= testid %>/<%= classId %>" method="POST">
            <div>
                <label for="startTime">Start Time:</label>
                <input type="datetime-local" id="startTime" name="startTime" required>
            </div>
            <div>
                <label for="endTime">End Time:</label>
                <input type="datetime-local" id="endTime" name="endTime" required>
            </div>
            <div>
                <input type="hidden" id="examId" name="examId" value="<%= testid %>">
                <input type="hidden" id="classId" name="classId" value="<%= classId %>">
                <button type="submit">Assign</button>
            </div>
        </form>
        
    </section>
</body>

</html>